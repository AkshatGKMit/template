import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  rippleContainer: {
    position: 'absolute',
    zIndex: -1,
    height: '100%',
    width: '100%',
  },
  parentContainer: {
    padding: 4,
  },
});

export default styles;
